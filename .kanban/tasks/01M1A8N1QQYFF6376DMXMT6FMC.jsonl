{"id":"01M1A8N1QRA5B982516X2PWYH1","timestamp":"2026-08-30T21:19:19.800188Z","op":"create","item_id":"01M1A8N1QQYFF6376DMXMT6FMC","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,59 @@\n+---\n+assignees:\n+- claude-code\n+depends_on:\n+- 01M1A8MDQ9NTZX4A0N8JZBA4XN\n+position_column: todo\n+position_ordinal: '8180'\n+title: 'Real-model test: a cold MetadataSearcher returns only catalog ids'\n+---\n+## What\n+\n+The first of the two real-model tests. It is the highest-value one on the board: it is the only thing that would catch the class of defect FoundationModelsRanker hit in `d333d07`.\n+\n+File to create:\n+- `IntegrationTests/Tests/FoundationModelsMetadataRegistryIntegrationTests/ColdSelectionRealModelTests.swift`\n+\n+Build a `MetadataSearcher` in `.selection` mode over the fixture catalog from `^jzba4xn`, with:\n+\n+```swift\n+SelectionConfig(\n+    model: { instructions in\n+        LanguageModelSession(model: .default, instructions: instructions)\n+    },\n+    preamble: .librarianDefault\n+)\n+```\n+\n+Then run `search(intent:limit:)` and assert every returned id is in the catalog, and that no `.unknownSelectedId` diagnostic fired — collect diagnostics through the `onDiagnostic:` parameter.\n+\n+### Two design constraints that are the whole point of this test\n+\n+**1. A FRESH `MetadataSearcher` per query.** Not one searcher reused across a parameterized run. FoundationModelsRanker measured this exact defect: on a cold session the on-device model returned `{\"ids\":[]}` for some queries, 0 of 5 runs succeeding, while the same query on a warm session succeeded every time. A suite that reuses one searcher **cannot see it**, because `LanguageModelSession.fork()` returns `self`, so the session is warm from call two onward. Construct the searcher inside the test body, per query.\n+\n+**2. At least one query phrased as an imperative.** The failing queries in Ranker's data were exactly the ones phrased as an order — \"record my staged changes as a new commit\" scored 0/5 before the fix. Interrogative phrasings (\"how do I list or delete a branch\") were less affected. Use a parameterized test with a mix, and make sure an imperative is among them.\n+\n+Treat Ranker's causal story as unproven — they were explicit that restating the framing in plain prose worked worse than the Markdown heading, so *why* it helps is not established, over four fixture queries and one model version. The structural point stands regardless: cold, imperative, one query at a time is the shape that fails.\n+\n+### Scope\n+\n+Under-budget cached-root path only. Do not add an over-budget case; that path's mechanics are already at 97% line coverage through fakes, and the model-behavior half belongs to FoundationModelsRanker.\n+\n+## Acceptance Criteria\n+\n+- [ ] The test constructs a new `MetadataSearcher` for every query, inside the test body.\n+- [ ] At least one query is phrased as an imperative.\n+- [ ] Every returned id is asserted to be a member of the fixture catalog's id set.\n+- [ ] No `.unknownSelectedId` diagnostic is recorded for any query.\n+- [ ] At least one match is returned for each on-topic query — an empty result is a failure, because empty is the exact symptom of the defect this test guards.\n+- [ ] The test calls `ModelAvailability.requireAvailable()` first.\n+\n+## Tests\n+\n+- [ ] `IntegrationTests/Tests/.../ColdSelectionRealModelTests.swift` — a parameterized `@Test(arguments:)` over three or more intents, at least one imperative.\n+- [ ] Run `swift test --package-path IntegrationTests`. All queries return at least one valid catalog id.\n+- [ ] Run it three times. Record the pass rate per query in a comment on this card. A query that is not stable across three runs must be replaced or its flakiness recorded, never silently retried.\n+- [ ] Prove the assertion can fail: temporarily assert against a catalog id set that excludes a real answer, confirm the failure, revert.\n+\n+## Workflow\n+- Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,59 +0,0 @@\n----\n-assignees:\n-- claude-code\n-depends_on:\n-- 01M1A8MDQ9NTZX4A0N8JZBA4XN\n-position_column: todo\n-position_ordinal: '8180'\n-title: 'Real-model test: a cold MetadataSearcher returns only catalog ids'\n----\n-## What\n-\n-The first of the two real-model tests. It is the highest-value one on the board: it is the only thing that would catch the class of defect FoundationModelsRanker hit in `d333d07`.\n-\n-File to create:\n-- `IntegrationTests/Tests/FoundationModelsMetadataRegistryIntegrationTests/ColdSelectionRealModelTests.swift`\n-\n-Build a `MetadataSearcher` in `.selection` mode over the fixture catalog from `^jzba4xn`, with:\n-\n-```swift\n-SelectionConfig(\n-    model: { instructions in\n-        LanguageModelSession(model: .default, instructions: instructions)\n-    },\n-    preamble: .librarianDefault\n-)\n-```\n-\n-Then run `search(intent:limit:)` and assert every returned id is in the catalog, and that no `.unknownSelectedId` diagnostic fired — collect diagnostics through the `onDiagnostic:` parameter.\n-\n-### Two design constraints that are the whole point of this test\n-\n-**1. A FRESH `MetadataSearcher` per query.** Not one searcher reused across a parameterized run. FoundationModelsRanker measured this exact defect: on a cold session the on-device model returned `{\"ids\":[]}` for some queries, 0 of 5 runs succeeding, while the same query on a warm session succeeded every time. A suite that reuses one searcher **cannot see it**, because `LanguageModelSession.fork()` returns `self`, so the session is warm from call two onward. Construct the searcher inside the test body, per query.\n-\n-**2. At least one query phrased as an imperative.** The failing queries in Ranker's data were exactly the ones phrased as an order — \"record my staged changes as a new commit\" scored 0/5 before the fix. Interrogative phrasings (\"how do I list or delete a branch\") were less affected. Use a parameterized test with a mix, and make sure an imperative is among them.\n-\n-Treat Ranker's causal story as unproven — they were explicit that restating the framing in plain prose worked worse than the Markdown heading, so *why* it helps is not established, over four fixture queries and one model version. The structural point stands regardless: cold, imperative, one query at a time is the shape that fails.\n-\n-### Scope\n-\n-Under-budget cached-root path only. Do not add an over-budget case; that path's mechanics are already at 97% line coverage through fakes, and the model-behavior half belongs to FoundationModelsRanker.\n-\n-## Acceptance Criteria\n-\n-- [ ] The test constructs a new `MetadataSearcher` for every query, inside the test body.\n-- [ ] At least one query is phrased as an imperative.\n-- [ ] Every returned id is asserted to be a member of the fixture catalog's id set.\n-- [ ] No `.unknownSelectedId` diagnostic is recorded for any query.\n-- [ ] At least one match is returned for each on-topic query — an empty result is a failure, because empty is the exact symptom of the defect this test guards.\n-- [ ] The test calls `ModelAvailability.requireAvailable()` first.\n-\n-## Tests\n-\n-- [ ] `IntegrationTests/Tests/.../ColdSelectionRealModelTests.swift` — a parameterized `@Test(arguments:)` over three or more intents, at least one imperative.\n-- [ ] Run `swift test --package-path IntegrationTests`. All queries return at least one valid catalog id.\n-- [ ] Run it three times. Record the pass rate per query in a comment on this card. A query that is not stable across three runs must be replaced or its flakiness recorded, never silently retried.\n-- [ ] Prove the assertion can fail: temporarily assert against a catalog id set that excludes a real answer, confirm the failure, revert.\n-\n-## Workflow\n-- Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01M1A983MSZEH9B7NCA415TFR5","timestamp":"2026-08-30T21:29:44.345483Z","op":"update","item_id":"01M1A8N1QQYFF6376DMXMT6FMC","forward_patch":"--- original\n+++ modified\n@@ -2,7 +2,7 @@\n assignees:\n - claude-code\n depends_on:\n-- 01M1A8MDQ9NTZX4A0N8JZBA4XN\n+- 01M1A97K9T94SEZ0CA0NWT7NZ4\n position_column: todo\n position_ordinal: '8180'\n title: 'Real-model test: a cold MetadataSearcher returns only catalog ids'\n@@ -9,12 +9,14 @@\n ---\n ## What\n\n-The first of the two real-model tests. It is the highest-value one on the board: it is the only thing that would catch the class of defect FoundationModelsRanker hit in `d333d07`.\n+The first real-model test, and the highest-value card on the board: the only thing that would catch the class of defect FoundationModelsRanker hit in `d333d07`.\n\n File to create:\n - `IntegrationTests/Tests/FoundationModelsMetadataRegistryIntegrationTests/ColdSelectionRealModelTests.swift`\n\n-Build a `MetadataSearcher` in `.selection` mode over the fixture catalog from `^jzba4xn`, with:\n+Use **only the intents `^nwt7nz4` measured at 5/5**. Do not invent new ones here.\n+\n+Build a `MetadataSearcher` in `.selection` mode over `IntegrationCatalog`'s base group:\n\n ```swift\n SelectionConfig(\n@@ -25,35 +27,40 @@\n )\n ```\n\n-Then run `search(intent:limit:)` and assert every returned id is in the catalog, and that no `.unknownSelectedId` diagnostic fired — collect diagnostics through the `onDiagnostic:` parameter.\n+Collect diagnostics through `onDiagnostic:`. Wrap the model call in `ModelAvailability.recordingEnvironmentFaults(_:)` so a `GenerationError` reads as an environment fault, not a product defect.\n+\n+### A FRESH searcher per query — the whole point\n\n-### Two design constraints that are the whole point of this test\n+Construct the searcher **inside the test body**, per query. Not one searcher reused across a parameterized run.\n\n-**1. A FRESH `MetadataSearcher` per query.** Not one searcher reused across a parameterized run. FoundationModelsRanker measured this exact defect: on a cold session the on-device model returned `{\"ids\":[]}` for some queries, 0 of 5 runs succeeding, while the same query on a warm session succeeded every time. A suite that reuses one searcher **cannot see it**, because `LanguageModelSession.fork()` returns `self`, so the session is warm from call two onward. Construct the searcher inside the test body, per query.\n+Ranker measured this exact defect: on a cold session the model returned `{\"ids\":[]}` for some queries, 0 of 5 runs succeeding, while the same query on a warm session succeeded every time. A suite that reuses one searcher **cannot see it**, because `LanguageModelSession.fork()` returns `self` (`LanguageModelSessionSupport.swift:100-102`), so the session is warm from call two onward.\n\n-**2. At least one query phrased as an imperative.** The failing queries in Ranker's data were exactly the ones phrased as an order — \"record my staged changes as a new commit\" scored 0/5 before the fix. Interrogative phrasings (\"how do I list or delete a branch\") were less affected. Use a parameterized test with a mix, and make sure an imperative is among them.\n+### What actually gets asserted\n\n-Treat Ranker's causal story as unproven — they were explicit that restating the framing in plain prose worked worse than the Markdown heading, so *why* it helps is not established, over four fixture queries and one model version. The structural point stands regardless: cold, imperative, one query at a time is the shape that fails.\n+**Membership is a tautology — do not make it the headline.** `SelectionTier` already filters every unresolvable id before returning and reports it via `.unknownSelectedId` (`SelectionTier.swift:286-289`), so `search(intent:limit:)` *cannot* return a non-catalog id. Keep the membership check as a cheap invariant, but the two observables that can genuinely change are:\n\n+1. the result is **non-empty**, and\n+2. **no `.unknownSelectedId` fired**.\n+\n+Those are the criteria.\n+\n ### Scope\n\n-Under-budget cached-root path only. Do not add an over-budget case; that path's mechanics are already at 97% line coverage through fakes, and the model-behavior half belongs to FoundationModelsRanker.\n+Under-budget cached-root path only. No over-budget case: those mechanics are at 97% line coverage via fakes, and the model-behavior half belongs to Ranker.\n\n ## Acceptance Criteria\n\n-- [ ] The test constructs a new `MetadataSearcher` for every query, inside the test body.\n-- [ ] At least one query is phrased as an imperative.\n-- [ ] Every returned id is asserted to be a member of the fixture catalog's id set.\n+- [ ] A new `MetadataSearcher` is constructed for every query, inside the test body.\n+- [ ] Every intent used comes from `^nwt7nz4`'s 5/5 list, and at least one is an imperative.\n+- [ ] Each on-topic query returns a **non-empty** result — empty is the exact symptom of the guarded defect.\n - [ ] No `.unknownSelectedId` diagnostic is recorded for any query.\n-- [ ] At least one match is returned for each on-topic query — an empty result is a failure, because empty is the exact symptom of the defect this test guards.\n-- [ ] The test calls `ModelAvailability.requireAvailable()` first.\n+- [ ] Model calls are wrapped in `recordingEnvironmentFaults(_:)`, and the test calls `try ModelAvailability.requireAvailable()` first.\n\n ## Tests\n\n-- [ ] `IntegrationTests/Tests/.../ColdSelectionRealModelTests.swift` — a parameterized `@Test(arguments:)` over three or more intents, at least one imperative.\n-- [ ] Run `swift test --package-path IntegrationTests`. All queries return at least one valid catalog id.\n-- [ ] Run it three times. Record the pass rate per query in a comment on this card. A query that is not stable across three runs must be replaced or its flakiness recorded, never silently retried.\n-- [ ] Prove the assertion can fail: temporarily assert against a catalog id set that excludes a real answer, confirm the failure, revert.\n+- [ ] `ColdSelectionRealModelTests.swift` — a parameterized `@Test(arguments:)` over the chosen intents.\n+- [ ] Run `swift test --package-path IntegrationTests` three times. All queries non-empty every run. Record the rate in a comment; a query not stable across three runs is replaced from `^nwt7nz4`'s list, never retried in a loop.\n+- [ ] Prove the non-empty assertion can fail: temporarily point the searcher at an empty catalog, confirm failure, revert. This mutates the product input, not the expectation.\n\n ## Workflow\n - Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -2,7 +2,7 @@\n assignees:\n - claude-code\n depends_on:\n-- 01M1A97K9T94SEZ0CA0NWT7NZ4\n+- 01M1A8MDQ9NTZX4A0N8JZBA4XN\n position_column: todo\n position_ordinal: '8180'\n title: 'Real-model test: a cold MetadataSearcher returns only catalog ids'\n@@ -9,14 +9,12 @@\n ---\n ## What\n\n-The first real-model test, and the highest-value card on the board: the only thing that would catch the class of defect FoundationModelsRanker hit in `d333d07`.\n+The first of the two real-model tests. It is the highest-value one on the board: it is the only thing that would catch the class of defect FoundationModelsRanker hit in `d333d07`.\n\n File to create:\n - `IntegrationTests/Tests/FoundationModelsMetadataRegistryIntegrationTests/ColdSelectionRealModelTests.swift`\n\n-Use **only the intents `^nwt7nz4` measured at 5/5**. Do not invent new ones here.\n-\n-Build a `MetadataSearcher` in `.selection` mode over `IntegrationCatalog`'s base group:\n+Build a `MetadataSearcher` in `.selection` mode over the fixture catalog from `^jzba4xn`, with:\n\n ```swift\n SelectionConfig(\n@@ -27,40 +25,35 @@\n )\n ```\n\n-Collect diagnostics through `onDiagnostic:`. Wrap the model call in `ModelAvailability.recordingEnvironmentFaults(_:)` so a `GenerationError` reads as an environment fault, not a product defect.\n+Then run `search(intent:limit:)` and assert every returned id is in the catalog, and that no `.unknownSelectedId` diagnostic fired — collect diagnostics through the `onDiagnostic:` parameter.\n\n-### A FRESH searcher per query — the whole point\n-\n-Construct the searcher **inside the test body**, per query. Not one searcher reused across a parameterized run.\n+### Two design constraints that are the whole point of this test\n\n-Ranker measured this exact defect: on a cold session the model returned `{\"ids\":[]}` for some queries, 0 of 5 runs succeeding, while the same query on a warm session succeeded every time. A suite that reuses one searcher **cannot see it**, because `LanguageModelSession.fork()` returns `self` (`LanguageModelSessionSupport.swift:100-102`), so the session is warm from call two onward.\n-\n-### What actually gets asserted\n-\n-**Membership is a tautology — do not make it the headline.** `SelectionTier` already filters every unresolvable id before returning and reports it via `.unknownSelectedId` (`SelectionTier.swift:286-289`), so `search(intent:limit:)` *cannot* return a non-catalog id. Keep the membership check as a cheap invariant, but the two observables that can genuinely change are:\n+**1. A FRESH `MetadataSearcher` per query.** Not one searcher reused across a parameterized run. FoundationModelsRanker measured this exact defect: on a cold session the on-device model returned `{\"ids\":[]}` for some queries, 0 of 5 runs succeeding, while the same query on a warm session succeeded every time. A suite that reuses one searcher **cannot see it**, because `LanguageModelSession.fork()` returns `self`, so the session is warm from call two onward. Construct the searcher inside the test body, per query.\n\n-1. the result is **non-empty**, and\n-2. **no `.unknownSelectedId` fired**.\n+**2. At least one query phrased as an imperative.** The failing queries in Ranker's data were exactly the ones phrased as an order — \"record my staged changes as a new commit\" scored 0/5 before the fix. Interrogative phrasings (\"how do I list or delete a branch\") were less affected. Use a parameterized test with a mix, and make sure an imperative is among them.\n\n-Those are the criteria.\n+Treat Ranker's causal story as unproven — they were explicit that restating the framing in plain prose worked worse than the Markdown heading, so *why* it helps is not established, over four fixture queries and one model version. The structural point stands regardless: cold, imperative, one query at a time is the shape that fails.\n\n ### Scope\n\n-Under-budget cached-root path only. No over-budget case: those mechanics are at 97% line coverage via fakes, and the model-behavior half belongs to Ranker.\n+Under-budget cached-root path only. Do not add an over-budget case; that path's mechanics are already at 97% line coverage through fakes, and the model-behavior half belongs to FoundationModelsRanker.\n\n ## Acceptance Criteria\n\n-- [ ] A new `MetadataSearcher` is constructed for every query, inside the test body.\n-- [ ] Every intent used comes from `^nwt7nz4`'s 5/5 list, and at least one is an imperative.\n-- [ ] Each on-topic query returns a **non-empty** result — empty is the exact symptom of the guarded defect.\n+- [ ] The test constructs a new `MetadataSearcher` for every query, inside the test body.\n+- [ ] At least one query is phrased as an imperative.\n+- [ ] Every returned id is asserted to be a member of the fixture catalog's id set.\n - [ ] No `.unknownSelectedId` diagnostic is recorded for any query.\n-- [ ] Model calls are wrapped in `recordingEnvironmentFaults(_:)`, and the test calls `try ModelAvailability.requireAvailable()` first.\n+- [ ] At least one match is returned for each on-topic query — an empty result is a failure, because empty is the exact symptom of the defect this test guards.\n+- [ ] The test calls `ModelAvailability.requireAvailable()` first.\n\n ## Tests\n\n-- [ ] `ColdSelectionRealModelTests.swift` — a parameterized `@Test(arguments:)` over the chosen intents.\n-- [ ] Run `swift test --package-path IntegrationTests` three times. All queries non-empty every run. Record the rate in a comment; a query not stable across three runs is replaced from `^nwt7nz4`'s list, never retried in a loop.\n-- [ ] Prove the non-empty assertion can fail: temporarily point the searcher at an empty catalog, confirm failure, revert. This mutates the product input, not the expectation.\n+- [ ] `IntegrationTests/Tests/.../ColdSelectionRealModelTests.swift` — a parameterized `@Test(arguments:)` over three or more intents, at least one imperative.\n+- [ ] Run `swift test --package-path IntegrationTests`. All queries return at least one valid catalog id.\n+- [ ] Run it three times. Record the pass rate per query in a comment on this card. A query that is not stable across three runs must be replaced or its flakiness recorded, never silently retried.\n+- [ ] Prove the assertion can fail: temporarily assert against a catalog id set that excludes a real answer, confirm the failure, revert.\n\n ## Workflow\n - Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n","transaction_id":null}
