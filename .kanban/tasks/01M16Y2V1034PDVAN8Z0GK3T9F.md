---
assignees:
- claude-code
comments:
- actor: claude-code
  id: 01m170hy9dr9kd3bkfa642v292
  text: |-
    Picked up. Research notes:

    - `DeterministicEmbedder` is declared only in `Examples/HotReloadCore/HotReloadCore.swift`, in the `// MARK: - GPU-free deterministic embedder` section. No test names `HotReloadCore.DeterministicEmbedder` today, so no test rename is needed.
    - The only other references are the two doc comments in that same file: the file header (says the type is "defined here") and the `embedder:` parameter doc of `runHotReloadBurst`.
    - `TextEmbedding` comes from `FoundationModelsRanker` and is re-exported by the main library target (`Sources/FoundationModelsMetadataRegistry/FoundationModelsRankerReexport.swift`). `ExamplesSupport` already depends on the main library target only, so the move needs NO new dependency in `Package.swift`.
    - `ExamplesSmokeTests.swift` already qualifies calls by module (`ExamplesSupport.formattedMatches`, `CatalogSearchCore.runCatalogSearch`). The new test therefore says `ExamplesSupport.DeterministicEmbedder(...)`, which is a true RED: an unqualified name would resolve to `HotReloadCore` today and pass before the move.
    - swiftlint and periphery are both installed and there is no `.swiftlint.yml`, so `no_magic_numbers` reads the new test. The dimension `8` is used twice in the test, so it becomes a named constant.
  timestamp: 2026-08-29T15:00:06.061989+00:00
- actor: claude-code
  id: 01m170vdk4m1ft3q0tqcz04e1k
  text: |-
    Implementation landed, TDD order kept.

    RED: added the two new tests first, spelled `ExamplesSupport.DeterministicEmbedder(...)`. `swift build --build-tests` failed with `module 'ExamplesSupport' has no member named 'DeterministicEmbedder'` at both call sites — the correct failure.

    GREEN: moved the type. `swift test` reports 104 tests in 9 suites, 0 failures, and the two new tests pass.

    What changed:
    - `Examples/ExamplesSupport/DeterministicEmbedder.swift` — new. Holds the `// MARK: - GPU-free deterministic embedder` section, the type, and its doc comments word for word, plus `import FoundationModelsMetadataRegistry` for `TextEmbedding`.
    - `Examples/HotReloadCore/HotReloadCore.swift` — deleted the moved section. `import ExamplesSupport` was already there. One doc-comment line changed for accuracy: the file header said the embedder is "defined here", which the move made false; it now names `ExamplesSupport` and keeps the same reason.
    - `Tests/.../ExamplesSmokeTests.swift` — two new tests plus two private static constants.
    - `Package.swift` — untouched. `ExamplesSupport` still depends on the main library target only.

    Acceptance criteria:
    - `DeterministicEmbedder` is declared in `Examples/ExamplesSupport/DeterministicEmbedder.swift` and nowhere else (`grep -rn "struct DeterministicEmbedder"` returns that one line).
    - `HotReloadCore` compiles, and `runHotReloadBurst` still defaults `embedder:` to `DeterministicEmbedder()`.
    - `Package.swift` gains no dependency (`git diff --stat Package.swift` is empty).
    - The existing `HotReloadTests` burst tests and the `ExamplesSmokeTests` burst tests pass unchanged.

    Note for the next agent: the card asked for one test asserting both claims. It is written as two tests, because the `/tdd` rule splits a name that carries an "and". Both assertions the card names are present: two vectors of length 8, and the same input twice giving the same output.

    Rule notes: the dimension and the input texts are named constants, because swiftlint `no_magic_numbers` reads test files (Swift Testing structs are not `XCTestCase`, so the tool's test carve-out never fires) and this repository carries no `.swiftlint.yml`. Running the three shipped swiftlint rules over the three files reports nothing on the new file and nothing on the new test code; two pre-existing `limit: 5` literals in `runSelectionRootRebuildDemo` (untouched lines) still report.
  timestamp: 2026-08-29T15:05:16.644503+00:00
- actor: claude-code
  id: 01m170vmqnq9dh410bg0gmej4z
  text: |-
    ### implement — changed
    - evidence: 3 files — /Users/wballard/github/swissarmyhammer/FoundationModelsMetadataRegistry/Examples/ExamplesSupport/DeterministicEmbedder.swift (new), /Users/wballard/github/swissarmyhammer/FoundationModelsMetadataRegistry/Examples/HotReloadCore/HotReloadCore.swift, /Users/wballard/github/swissarmyhammer/FoundationModelsMetadataRegistry/Tests/FoundationModelsMetadataRegistryTests/ExamplesSmokeTests.swift. `swift build --build-tests` RED: 2 errors, "module 'ExamplesSupport' has no member named 'DeterministicEmbedder'". `swift test` GREEN: 104 tests in 9 suites, 0 failures. `swift build --build-tests` after the move: 0 errors, 0 warnings. Package.swift unchanged.
    - next: /review
  timestamp: 2026-08-29T15:05:23.957996+00:00
depends_on:
- 01M16Y2F9XS7DWS7SQCC64BDJS
position_column: doing
position_ordinal: '80'
title: Move DeterministicEmbedder into ExamplesSupport
---
## What

`DeterministicEmbedder` is the GPU-free `TextEmbedding` that replaces the live-Router embedder in every demo. It is public today, but it lives in one demo target: `Examples/HotReloadCore/HotReloadCore.swift:73`.

Move it to the shared `Examples/ExamplesSupport/` target so `SemanticSearchCore` can use it too. `ExamplesSupport` depends only on the main library target, so it stays GPU-free and Router-free.

Files to change:
- `Examples/ExamplesSupport/DeterministicEmbedder.swift` — new file. Move the type, its doc comment, and the `// MARK: - GPU-free deterministic embedder` section from `HotReloadCore.swift:63-...` unchanged.
- `Examples/HotReloadCore/HotReloadCore.swift` — delete the moved type. Add `import ExamplesSupport` if it is absent.
- `Tests/FoundationModelsMetadataRegistryTests/` — update any test that names `HotReloadCore.DeterministicEmbedder`.

Do not change the type's behavior. This is a move only.

## Acceptance Criteria

- [ ] `DeterministicEmbedder` is declared in `Examples/ExamplesSupport/DeterministicEmbedder.swift` and nowhere else.
- [ ] `HotReloadCore` still compiles and still defaults its `embedder` parameter to `DeterministicEmbedder()`.
- [ ] `ExamplesSupport` gains no new target dependency in `Package.swift`.

## Tests

- [ ] `Tests/FoundationModelsMetadataRegistryTests/HotReloadTests.swift` — the existing burst tests still pass unchanged, proving the moved embedder behaves the same.
- [ ] Add a test in `Tests/FoundationModelsMetadataRegistryTests/ExamplesSmokeTests.swift` that calls `DeterministicEmbedder(dimension: 8).embed(["a", "b"])` and asserts two vectors of length 8, and that the same input gives the same output twice.
- [ ] Run `swift test`. All tests pass.

## Workflow
- Use `/tdd` — write failing tests first, then implement to make them pass.