---
assignees:
- claude-code
comments:
- actor: claude-code
  id: 01m17bjxe6s610tkb2y32qpy26
  text: |-
    ### BLOCKED — external, do not start

    The board shows this card READY because its kanban dependency `^b2487hc` is done. It is NOT actually startable. The real blocker is outside this repository.

    Reported by the FoundationModelsRanker session on 2026-08-29:

    - Ranker's local branch is **10 commits ahead of `origin/main`, and nothing is pushed**. That session cannot push; the decision belongs to its user.
    - The API this card migrates to exists **only on that machine**: `SelectionConfig.model` became one-argument in local commit `392bb5f`, and `idEnumSchema(ids:)` landed in local commit `00da13c`. The manifest card `^2q0p8gr` (`dependencies: []`) was still running.
    - **Re-pinning FoundationModelsRanker `main` right now fetches the OLD two-argument API and breaks the build.** Do not re-pin until that session reports an actual push.

    That session will message this one when a push happens.

    ### Extra work to fold into this card when it unblocks

    Ranker found a real defect in the shared lineage, fixed locally in `cbcee8c`: `SelectionTier.assemblePrefix` rendered only `summaryBlock(forID:)` per candidate, so the assembled prompt showed the model **no ids at all**, while the preamble instructed it not to invent ids. On their default on-device path every query returned no matches and reported `unknownSelectedId`. A Router-backed caller never saw it, because the id-enum grammar forced a valid id out of the decoder no matter what the prompt said.

    Their fix renders each candidate as `## <id>` above its summary.

    This package drives Ranker's `SelectionTier`, and `OverBudgetTests` calls `SelectionTier.assemblePrefix` directly. The fix should therefore arrive with the re-pin — but verify it did, and confirm the assembled prefix contains the candidate ids, before relying on the selection path without a grammar.
  timestamp: 2026-08-29T18:12:52.294065+00:00
- actor: claude-code
  id: 01m19969y7ewwgpyg2dme0cdmf
  text: |-
    ### Blocker cleared — upstream API verified before migrating

    `swift package update FoundationModelsRanker` resolved `main` at `35447e4`. That commit is a descendant of the reported `222e74a1` (`git merge-base --is-ancestor` says YES), so the checkout is at or ahead of the pushed Router-free API.

    Verified all four conditions in `.build/checkouts/FoundationModelsRanker`:

    1. `SelectionConfig.init(model:)` is `@escaping @Sendable (String) -> any AgentSession` — ONE argument, no `Grammar`. Note the struct no longer stores a `model` property; it stores `sessionSource: SelectionSessionSource`, an enum with `.factory(@Sendable (String) -> any AgentSession)` and a new `.session(any AgentSession)` case. The `init(model:)` form this package uses is unchanged apart from the dropped argument.
    2. `SelectionTier.idEnumSchema(ids:) throws -> String` exists. It **throws** — the card wrote it unthrown. `idEnumGrammar` is gone. A new `SelectionSchemaShapeError` is the shape failure.
    3. `RoutedEmbedderAdapter` and `RoutedAgentSession` are gone. The only `Grammar` token left in Ranker's sources is one doc comment showing a caller how to build a grammar from the schema text.
    4. Ranker's `Package.swift` has package-level `dependencies: []`.

    ### Upstream defect fix confirmed present

    `SelectionTier.assemblePrefix` now routes both overloads through one private `candidateEntry(forID:catalog:)` that returns `"## \(id)\n\(summary)"`. The assembled prefix therefore contains each candidate id as a markdown heading. This is the fix for the defect described in the previous comment.

    Proceeding with the migration.
  timestamp: 2026-08-30T12:09:30.823826+00:00
- actor: claude-code
  id: 01m199cbw86nfx0vatvmp72hm0
  text: |-
    ### Research — the full call-site inventory

    The card lists four files. There are **seven** that must change, plus two doc-comment sites the acceptance criteria reach.

    `SelectionConfig(model: { _, _ in ... })` — 11 closure sites:
    - `Tests/.../SelectionTests.swift` (1)
    - `Tests/.../OverBudgetTests.swift` (5)
    - `Tests/.../HotReloadTests.swift` (3)
    - `Examples/ExamplesSupport/DemoAgentSession.swift` (1)
    - `Examples/HotReloadCore/HotReloadCore.swift` (1) — **not on the card**, but it has the same two-argument closure and would break the build.

    `receivedGrammars` is **read by two tests**, so it cannot simply change type:
    - `OverBudgetTests.factoryReceivesAGrammarEnumConstrainedToTheCandidateSetAndCappedAtItsCount`
    - `HotReloadTests` over-budget embed-catch-up test (asserts the candidate set is `(z, x)`)

    The tier no longer derives or hands any schema to the factory — the factory receives instructions alone — so there is nothing left for the fixture to record. `receivedGrammarsBox`/`receivedGrammars` therefore **go away** rather than become `[String]`.

    Both tests keep their intent through the surviving observable: `receivedInstructions`. The upstream `assemblePrefix` fix puts each candidate id in the prefix as a `## <id>` heading, so "this round's candidate set is exactly (alpha, bravo)" and "(z, x)" are both assertable from the instructions text. That is the same fact the grammar enum used to carry, and it is the fact that now matters, because no grammar constrains the decoder any more.

    Two doc-comment sites also name the forbidden symbols (acceptance criterion 2):
    - `Sources/FoundationModelsMetadataRegistry/FoundationModelsRankerReexport.swift` names `RoutedEmbedderAdapter` and `RoutedAgentSession`
    - `Tests/.../EmbeddingTests.swift` names `RoutedEmbedderAdapter` and `FoundationModelsRouter`

    The four existing `idEnumGrammar` tests in `SelectionTests` and the one in `HotReloadTests` already parse with `JSONSerialization` — they only had to unwrap `.jsonSchema(let source)` from the `Grammar` first. With `idEnumSchema` returning the source text directly, that unwrap goes and the parse stays. The repeated five-line parse chain across five tests is duplication that the shared helper removes.
  timestamp: 2026-08-30T12:12:49.416453+00:00
- actor: claude-code
  id: 01m199vxbrq5rkjgna40pkv7xk
  text: |-
    ### Migration landed

    **Acceptance criteria**

    1. No file in `Tests/` or `Examples/` imports `FoundationModelsRouter` — met. `TestSupport/SelectionFixtures.swift:1` was the last one in the repository.
    2. No file names `Grammar`, `idEnumGrammar`, `RoutedEmbedderAdapter`, or `RoutedAgentSession` — met across `Sources/`, `Tests/` and `Examples/`. Two files still name them and both are owned by downstream cards, so this card deliberately leaves them: `Package.swift` (doc comments on the `dependencies:` entries this card is told not to touch — `^3aeakq4`) and `plan.md` (the design document — `^ek17grh`, "Update the docs that describe the Router dependency"). `Package.swift` cannot satisfy the criterion while the Router dependency stands at all: the manifest holds `let routerDependencyName = "FoundationModelsRouter"`, so the name is load-bearing until that entry goes.
    3. `Package.resolved` pins a Ranker revision whose manifest lists no dependencies — met: `35447e4c77bc825dec26d4879678359bc291b23d`, package-level `dependencies: []`. Note this repository **gitignores `Package.resolved`** (`.gitignore:8`), so the re-pin will not show in the commit. That is the repository's own policy, not a gap.
    4. `swift build` completes with no error — met. The only warning is the expected `dependency 'foundationmodelsrouter' is not used by any target`, which `^3aeakq4` clears.

    **Tests: 108 pass in 10 suites, zero failures.** The count moved 109 → 108: four `idEnumGrammar*` tests in `SelectionTests` collapsed into the two the card specifies (the function is pure in `ids`, so one three-id input covers what three catalog-input tests covered), and `OverBudgetTests` traded one grammar test for two prefix tests.

    **What replaced the grammar assertions.** The tier no longer derives or hands a schema to the session factory, so `receivedGrammars`/`receivedGrammarsBox` had nothing left to record and were deleted rather than retyped to `[String]`. The two tests that read them now assert the same fact through `receivedInstructions`: with the upstream `assemblePrefix` fix, each candidate id appears as a `## <id>` heading, so "this round's candidate set is exactly (alpha, bravo)" and "(z, x)" are both readable off the prefix. That is now the *right* place to assert it — no grammar constrains the decoder any more, so the prefix is the only thing between the model and an invented id.

    **Step 3 — the upstream defect fix is verified live, not assumed.** Both new assertions were mutation-checked rather than merely observed passing:

    - `assembledPrefixNamesEachCandidateIdAboveItsSummary` inverted to the pre-fix expectation (`!prefix.contains("## <id>\n<summary>")`) → **fails with 5 issues**, one per catalog item. So the prefix genuinely carries every candidate id directly above its summary.
    - `overBudgetPrefixNamesExactlyThisRoundsCandidateIds` with its exclusion assertion inverted (`instructions.contains("## charlie")`) → **fails**. So the top-M cut is really what keeps charlie out, and the assertion is not vacuous.

    Both were reverted and are green.

    **Schema tests parse, never string-match**, per the card: a new `TestSupport/SelectionSchema.swift` decodes the source text with `JSONSerialization` into `SelectionIDConstraints { allowedIDs: Set<String>, uniqueItems: Bool, maxItems: Int }`. Five tests needed the same `properties`/`ids`/`items` walk, so the decode lives in one place. The empty-input case asserts both an empty enum and `maxItems == 0`.

    **Beyond the card's file list:** `Examples/HotReloadCore/HotReloadCore.swift` also had a two-argument closure and would have broken the build. `FoundationModelsRankerReexport.swift` and `EmbeddingTests.swift` named the deleted `Routed*` types in doc comments (criterion 2). `import Foundation` became unused in `HotReloadTests` and `SelectionTests` once the JSON walk left them, so both were dropped.

    **Checks run:** `swift build --build-tests` clean after `rm -rf .build/out`; `periphery scan` over the rebuilt index reports **no unused code**; `swift test` green.

    `swift-format lint` with default settings reports indentation findings across untouched files such as `SelectionPreamble.swift` and `Match.swift` — the repository has no `.swift-format` config and uses 4-space indent throughout, so that default config is not this project's gate. The new file follows the prevailing 4-space style.

    ### implement — changed
    - evidence: 9 files — `Tests/.../TestSupport/SelectionSchema.swift` (new), `Tests/.../TestSupport/SelectionFixtures.swift`, `Tests/.../SelectionTests.swift`, `Tests/.../OverBudgetTests.swift`, `Tests/.../HotReloadTests.swift`, `Tests/.../EmbeddingTests.swift`, `Sources/.../FoundationModelsRankerReexport.swift`, `Examples/ExamplesSupport/DemoAgentSession.swift`, `Examples/HotReloadCore/HotReloadCore.swift`; plus the gitignored `Package.resolved` re-pin. `swift test` = 108 passed / 0 failed in 10 suites; `swift build --build-tests` = 0 errors, 1 expected unused-dependency warning; `periphery scan` = 0 findings.
    - next: `/review`. `^3aeakq4` then removes the Router dependency entry and its manifest comments; `^ek17grh` updates `plan.md`.
  timestamp: 2026-08-30T12:21:18.840820+00:00
- actor: claude-code
  id: 01m19a6wr3d9fy3b96ypf91c5v
  text: |
    ### review — clean
    - evidence: `review sha HEAD~1..HEAD` (commit 811dc90) — 0 findings, 0 confirmed, 0 refuted, 7 validators attempted, 0 failed. 9 files reviewed; 4 `.kanban/` files excluded by `.reviewignore`. Fresh index: `rm -rf .build/out && swift build --build-tests` completed with exit code 0 before the scan, so no dead-code finding came from a stale index.
    - next: task moved to `done`. No review findings section was added, because there are none.
  timestamp: 2026-08-30T12:27:18.659544+00:00
- actor: claude-code
  id: 01m19a7q56yzrcf38myg64e0g4
  text: |-
    ### finish iteration 1 — clean
    - unblocked: FoundationModelsRanker pushed. Re-pinned at 35447e4, a descendant of the 222e74a1 first reported. All four preconditions verified before migrating.
    - implement: changed — 9 files (1 new: Tests/.../TestSupport/SelectionSchema.swift). The last `import FoundationModelsRouter` in the repository is gone.
    - test: green — swift test 108 passed, 0 failed, 10 suites (109 to 108 because four idEnumGrammar tests collapsed into two, and OverBudgetTests traded one for two); swift build --build-tests after rm -rf .build/out gives 0 errors and one expected unused-dependency warning; periphery scan clean
    - commit: 811dc90 refactor(ranker): move onto FoundationModelsRanker's router-free API
    - review: clean — 0 findings, 7 validators, task moved to done. The reviewer cleared .build/out before scanning.

    ### Two corrections to this card's original description
    - `idEnumSchema(ids:)` **throws**. The card wrote it as non-throwing.
    - `SelectionConfig` no longer stores a `model` property at all. It stores `sessionSource: SelectionSessionSource`, with `.factory` and a new `.session(any AgentSession)` case. The existing `init(model:preamble:capacityCharacterLimit:candidateLimit:)` keeps its signature and wraps into `.factory`, so call sites did not break.

    ### Upstream defect: confirmed fixed and now covered here
    Ranker's `SelectionTier.assemblePrefix` had rendered only `summaryBlock(forID:)` per candidate, so the assembled prompt showed the model no ids while the preamble said not to invent them. The id-enum grammar had been masking it; with the grammar gone it would have been fatal for this package.

    The fix is live: both overloads route through one `candidateEntry` returning `"## \(id)\n\(summary)"`. This was NOT accepted on inspection — two new assertions were mutation-checked. Inverting the prefix assertion to the pre-fix expectation fails with 5 issues, one per catalog item; inverting the over-budget exclusion assertion fails too. Both reverted and green.

    ### Two forced deviations from the card's file list
    - `Examples/HotReloadCore/HotReloadCore.swift` carried an eleventh two-argument closure the card did not list; it would have broken the build.
    - `receivedGrammars` could not become `[String]`. The tier now hands the factory instructions and nothing else, so there was nothing left to record; it is deleted. The two tests that read it assert the same candidate-set fact through `receivedInstructions`, which the new `## <id>` headings make possible.

    ### Note
    `Package.resolved` is gitignored here (.gitignore:8), so the re-pin is correct in the working tree but does not appear in the commit.
  timestamp: 2026-08-30T12:27:45.702382+00:00
depends_on:
- 01M16Y40YBN4GBKHS2FB2487HC
position_column: done
position_ordinal: '9980'
title: Adopt the Router-free FoundationModelsRanker seam
---
## What

FoundationModelsRanker is removing its own Router dependency on branch `main`. Its public API changes in three ways that this package must follow:

1. `SelectionConfig.model` changes from `@Sendable (String, Grammar) -> any AgentSession` to `@Sendable (String) -> any AgentSession`. The grammar argument is gone.
2. `SelectionTier.idEnumGrammar(ids:) -> Grammar` becomes `SelectionTier.idEnumSchema(ids:) -> String`, which returns JSON Schema source text.
3. `RoutedEmbedderAdapter` and `RoutedAgentSession` are deleted. `TextEmbedding` and `AgentSession` stay public and unchanged.

Ranker also adds `Searcher.init(_ items:, embedder:, session: any AgentSession, ...)`. The existing factory-form `init` keeps its signature, so `MetadataSearcher` does not have to change.

**Do not start this task until the Ranker session reports that its manifest task (`^2q0p8gr`) has landed and is pushed.** Ranker lands the seam change before the manifest change, so an early re-pin catches a half-migrated API. As of the last report, Ranker task 1 of 9 is committed locally as `4b5c8bb`, nothing is pushed, and remote `main` still has the old API.

### What `idEnumSchema(ids:)` returns

It returns the WHOLE response schema, not the per-element enum. It starts from `Selection.generationSchema` (the `@Generable` type is `Selection { ids: [String] }`), makes three injections, and re-encodes:

```json
{
  "type": "object",
  "properties": {
    "ids": {
      "type": "array",
      "items": { "type": "string", "enum": ["alpha", "bravo"] },
      "uniqueItems": true,
      "maxItems": 2
    }
  }
}
```

The `enum` sits at `properties.ids.items.enum`. `uniqueItems` and `maxItems` sit one level up at `properties.ids`. `maxItems` equals `ids.count` exactly. An empty `ids` array gives an empty `enum` and `maxItems: 0`; that is intended.

`maxItems` is load-bearing, not decoration. The xgrammar pipeline enforces `minItems`/`maxItems` but silently ignores `uniqueItems`. Without the cap, a compiled grammar permits an unbounded array of repeated enum members. Ranker observed a roughly 6150-token runaway on an off-topic intent. Keep the cap if this schema is ever compiled into a grammar.

Files to change:
- `Package.resolved` — re-pin `FoundationModelsRanker` to the Router-free `main`. Run `swift package update FoundationModelsRanker`.
- `Tests/FoundationModelsMetadataRegistryTests/TestSupport/SelectionFixtures.swift` — delete `import FoundationModelsRouter` (line 1). Change `makeSession(instructions:grammar:)` (line 112) to take instructions only. Change `receivedGrammarsBox`/`receivedGrammars` (lines 86, 102) from `[Grammar]` to `[String]`, or delete them if no test reads them.
- `Tests/FoundationModelsMetadataRegistryTests/SelectionTests.swift`, `OverBudgetTests.swift`, `HotReloadTests.swift` — update every `SelectionConfig(model:)` call site to the one-argument closure.
- `Examples/ExamplesSupport/DemoAgentSession.swift` — update its `SelectionConfig` wiring to the one-argument closure.

## Acceptance Criteria

- [ ] No file in `Tests/` or `Examples/` imports `FoundationModelsRouter`.
- [ ] No file names `Grammar`, `idEnumGrammar`, `RoutedEmbedderAdapter`, or `RoutedAgentSession`.
- [ ] `Package.resolved` pins a FoundationModelsRanker revision whose manifest lists no dependencies.
- [ ] `swift build` completes with no error.

## Tests

- [ ] `Tests/FoundationModelsMetadataRegistryTests/SelectionTests.swift` — every existing selection test passes against the one-argument seam, with no change in what it asserts about ranking.
- [ ] Add a schema test. **Parse the returned string with `JSONSerialization`. Do not assert on the raw text** — key order out of `JSONSerialization` is unstable, so two equal schemas compare unequal as text. Call `SelectionTier.idEnumSchema(ids: ["alpha", "bravo", "charlie"])`, decode it, and assert:
  - `properties.ids.items.enum` read into a `Set<String>` equals `["alpha", "bravo", "charlie"]`
  - `properties.ids.uniqueItems` is `true`
  - `properties.ids.maxItems` equals `3`
- [ ] Add an empty-input test: `idEnumSchema(ids: [])` gives an empty `enum` and `maxItems: 0`.
- [ ] Run `swift test`. All tests pass.

## Workflow
- Use `/tdd` — write failing tests first, then implement to make them pass.