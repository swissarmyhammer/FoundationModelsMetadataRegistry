.claude/settings.local.json
.sah/
.shell/
.validators/
.build/
.swiftpm/
.DS_Store
# Package.resolved is committed (not ignored): it pins the FoundationModelsRouter
# and mlx-swift-lm branch dependencies to specific commits so builds are
# reproducible.
