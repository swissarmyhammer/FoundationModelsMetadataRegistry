name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    uses: swissarmyhammer/workflows/.github/workflows/swift-ci.yaml@main
    with:
      integration-gate-env: METADATA_REGISTRY_INTEGRATION_TESTS
      integration-xctest-glob: "*MetadataRegistryTests.xctest"
      integration-metallib-glob: "*Cmlx*/default.metallib"
