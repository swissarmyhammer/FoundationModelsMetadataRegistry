/// Scaffold placeholder that lets the library target build.
///
/// FoundationModelsMetadataRegistry is a derived, in-memory search index over
/// someone else's registry of metadata (plan.md §10 naming note). The real
/// surface — `SearchableMetadata`, `MetadataIndex`, `MetadataSearcher` —
/// lands with the M1 catalog + retrieval core tasks.
