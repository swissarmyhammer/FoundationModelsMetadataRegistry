import FoundationModelsMetadataRegistry
import Testing

/// Placeholder scaffold test proving the library target builds and is importable.
///
/// Proves the `FoundationModelsMetadataRegistry` library target builds, links,
/// and is importable from a Swift Testing test target (plan.md §10). Replaced
/// by real tests as the catalog/retrieval core (M1) lands.
@Test func moduleImportsAndBuilds() {
    #expect(Bool(true))
}
