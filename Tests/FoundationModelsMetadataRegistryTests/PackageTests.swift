import FoundationModelsMetadataRegistry
import Testing

/// Placeholder scaffold test (plan.md §10): proves the
/// `FoundationModelsMetadataRegistry` library target builds, links, and is
/// importable from a Swift Testing test target. Replaced by real tests as
/// the catalog/retrieval core (M1) lands.
@Test func moduleImportsAndBuilds() {
    #expect(Bool(true))
}
